---
phase: 04-anomaly-detection
plan: "01"
subsystem: database
tags: [sqlalchemy, alembic, postgres, anomalies, schema]

# Dependency graph
requires:
  - phase: 03-cost-monitoring
    provides: billing_records table and billing.py model patterns (utcnow, UUID PK, mapped_column conventions)
provides:
  - Anomaly SQLAlchemy ORM model with 14 columns, unique constraint, and 3 indexes
  - anomalies PostgreSQL table created via Alembic migration 37031473bbfa
  - Persistent storage foundation for anomaly detection results, severity, status lifecycle, and user actions
affects: [04-02, 04-03, 04-04, 04-05, anomaly-detection-service, anomaly-api, anomaly-frontend]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Anomaly model follows billing.py conventions (Mapped[] type hints, mapped_column(), utcnow helper reuse, __table_args__ for constraints and indexes)
    - server_default added to status ('new') and expected ('false') for direct SQL insert safety
    - UniqueConstraint on (service_name, resource_group, detected_date) prevents duplicate anomaly records per detection cycle

key-files:
  created:
    - backend/migrations/versions/37031473bbfa_add_anomalies_table.py
  modified:
    - backend/app/models/billing.py

key-decisions:
  - "server_default='new' on status and server_default='false' on expected added to migration — allows direct SQL inserts without Python layer; new table so no existing rows affected"
  - "Anomaly model placed after IngestionAlert in billing.py — maintains chronological model addition order in the file"

patterns-established:
  - "Anomaly unique key: (service_name, resource_group, detected_date) — one anomaly record per service+group+day combination"
  - "Status lifecycle: new -> investigating -> resolved | dismissed (enforced at application layer)"

requirements-completed: [ANOMALY-01, ANOMALY-02, ANOMALY-03]

# Metrics
duration: 7min
completed: 2026-02-21
---

# Phase 4 Plan 01: Anomaly Detection Database Foundation Summary

**Anomaly SQLAlchemy model with 14-column schema, unique constraint on (service_name, resource_group, detected_date), 3 performance indexes, and applied Alembic migration creating anomalies table in PostgreSQL**

## Performance

- **Duration:** 7 min
- **Started:** 2026-02-21T09:47:00Z
- **Completed:** 2026-02-21T09:54:36Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments

- Added Anomaly class to billing.py following all established model conventions (Mapped[] typed columns, utcnow helper, UUID PK, __table_args__ with constraints and indexes)
- Generated and applied Alembic migration 37031473bbfa creating the anomalies table with all 14 columns, NOT NULL constraints, unique constraint, and 3 indexes
- All verification checks pass: psql schema inspection, index listing, Python import test, alembic current confirms head at new revision

## Task Commits

Each task was committed atomically:

1. **Task 1: Add Anomaly model to billing.py and generate Alembic migration** - `a2da223` (feat)

**Plan metadata:** _(to be added in final commit)_

## Files Created/Modified

- `backend/app/models/billing.py` - Added Anomaly ORM class (lines 85-108) after IngestionAlert; all required SQLAlchemy imports were already present
- `backend/migrations/versions/37031473bbfa_add_anomalies_table.py` - Alembic migration creating anomalies table; manually added server_default='new' to status and server_default='false' to expected

## Decisions Made

- `server_default='new'` and `server_default='false'` added manually to the autogenerated migration for status and expected columns — follows Phase 3 precedent (server_default='' for string columns on existing tables); here the table is new so not strictly required but added as best practice for direct SQL insert safety
- No new imports needed in billing.py — Boolean, Text, Date, UniqueConstraint, and Index were already imported; Decimal was already imported from Python's decimal module; date was already imported from datetime

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- anomalies table is live in PostgreSQL with correct schema, constraints, and indexes
- Anomaly model is importable and ready for use in detection service and API layer
- Ready for Phase 4 Plan 02: anomaly detection logic (30-day rolling baseline, severity classification, upsert writes to anomalies table)

---
*Phase: 04-anomaly-detection*
*Completed: 2026-02-21*

---
phase: 03-cost-monitoring
plan: "05"
type: execute
wave: 5
depends_on: ["03-04"]
files_modified: []
autonomous: false
requirements: [COST-01, COST-02, COST-03, COST-04, COST-05, COST-06]

must_haves:
  truths:
    - "User sees real spend figures (or $0.00 with empty chart if no billing data) on the dashboard without errors"
    - "Day-range toggle (30d/60d/90d) updates the chart and breakdown table"
    - "Dimension selector changes the breakdown table data"
    - "Export CSV download triggers a real file download in the browser"
    - "Top-resources table renders (or shows informative empty state)"
    - "All dashboard sections load without console errors or blank white boxes"
  artifacts:
    - path: "frontend/src/pages/DashboardPage.tsx"
      provides: "Verified, functional cost dashboard"
  key_links:
    - from: "Browser dashboard at http://localhost:5173"
      to: "backend /api/v1/costs/* endpoints"
      via: "TanStack Query fetching cost data on page load"
      pattern: "costs/summary|costs/trend|costs/breakdown|costs/top-resources"
---

<objective>
Human verification of the complete Phase 3 cost monitoring dashboard. Confirm all 6 COST requirements are satisfied end-to-end: spend summary KPIs, MoM comparison, trend chart with day-range toggle, breakdown table with dimension selector, top resources table, and CSV export.

Purpose: Automated testing cannot verify visual correctness, chart rendering, interactive state transitions, or real file downloads. This checkpoint ensures the user can see and interact with the dashboard before Phase 3 is marked complete.
Output: Human approval of the dashboard. No files modified.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-cost-monitoring/03-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify Phase 3 cost dashboard end-to-end</name>
  <action>Human verification of all 6 COST requirements in the browser</action>
  <what-built>
    Complete Phase 3 cost monitoring dashboard built across plans 03-01 through 03-04:
    - Schema migration: billing_records now has region, tag, resource_id, resource_name columns
    - Azure client: captures ResourceLocation, tenant_id tag, ResourceId from API
    - 6 backend endpoints: /costs/summary, /costs/trend, /costs/breakdown, /costs/top-resources, /costs/export
    - Dashboard KPI cards: MTD spend, projected month-end, prior month with MoM delta badge
    - Daily spend AreaChart with 30/60/90-day toggle
    - Cost breakdown table with dimension selector (service / resource group / region / tag)
    - Top-10 resources table
    - CSV export button
  </what-built>
  <how-to-verify>
    **Prerequisites:** Ensure the app is running:
    ```bash
    docker compose up -d
    # frontend dev server (if not running via docker):
    cd frontend && npm run dev
    ```

    **Step 1 — Login and navigate to Dashboard:**
    Visit http://localhost:5173 (or the frontend URL). Log in. You should land on the Dashboard page.

    **Step 2 — KPI Cards (COST-01, COST-02):**
    - Verify 3 cards appear: "Month-to-Date Spend", "Projected Month-End", "Prior Month"
    - Cards should show dollar values (may be $0.00 if no real billing data has been ingested)
    - The "Prior Month" card should show either a percentage change badge or "N/A" — NOT a crash or blank

    **Step 3 — Trend Chart (COST-03):**
    - A chart area should be visible (not a blank white box)
    - Click "60d" — confirm the button activates (highlighted)
    - Click "90d" — confirm the button activates
    - Click "30d" — return to default
    - If billing data exists, verify the chart renders data points; if empty, verify a flat line or empty chart (not an error)

    **Step 4 — Cost Breakdown (COST-04):**
    - A "Cost Breakdown" card with a table should be visible
    - A dropdown/select should be present labeled with the current dimension
    - Select "Resource Group" from the dropdown — table should reload
    - Select "Region" — table should reload
    - Select "Tag" — table should reload
    - If no data: "No cost data for this period" message should appear (not blank)

    **Step 5 — CSV Export (COST-06):**
    - Click the "Export CSV" button in the breakdown section
    - A file download dialog should appear in the browser (OR a file should download automatically)
    - Open the downloaded file — it should contain a header row ("dimension,total_cost_usd") and data rows (or just the header if no data)
    - The browser should NOT display the CSV content in a new tab

    **Step 6 — Top Resources (COST-05):**
    - A "Top 10 Most Expensive Resources" card should appear below the breakdown
    - If no ingestion has run with the new columns: verify the empty state message mentions "Run ingestion to populate resource data" (or similar)
    - If ingestion data exists with resource IDs: verify resource names appear in the table

    **Step 7 — Console errors check:**
    Open browser DevTools (F12) → Console tab. Reload the dashboard page.
    Confirm: zero red errors in the console (warnings about React devtools or similar are OK).

    **Step 8 — Auth check:**
    Try visiting http://localhost:8000/api/v1/costs/summary in the browser directly — should show `{"detail":"Not authenticated"}` (not the actual data).
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 8 verification steps pass.
    Or describe any issues found (e.g., "Step 5 failed: CSV opened in browser tab", "Step 3: chart is blank white box with no chart area").
  </resume-signal>
</task>

</tasks>

<verification>
Human verification is the verification step for this plan. All 8 steps above must pass.
</verification>

<success_criteria>
- All 6 COST requirements verified by the user in the browser
- No console errors on dashboard load
- CSV export triggers file download (not browser tab)
- Interactive elements (day toggle, dimension select) update data without page reload
- Empty states are informative, not blank or errored
</success_criteria>

<output>
After verification approval, create `.planning/phases/03-cost-monitoring/03-05-SUMMARY.md` following the summary template.
Mark Phase 3 complete in STATE.md.
</output>
